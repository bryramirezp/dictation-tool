numpy>=2.0
sounddevice>=0.5
faster-whisper>=1.2
# Pinned, even though faster-whisper pulls it in anyway: older releases fill the
# model cache with symlinks that ctranslate2 could not open on at least one
# machine, which is issue #1.
huggingface_hub>=1.0
pyperclip>=1.9
pyautogui>=0.9.54
pystray>=0.19
customtkinter>=5.2
pillow>=10.0
pynput>=1.7
psutil>=5.9

# Optional - only needed to use an NVIDIA graphics card.
# cuDNN is not listed because ctranslate2 already ships its own copy, and the
# CUDA Toolkit you may have installed system-wide is not used either.
# nvidia-cublas-cu12
